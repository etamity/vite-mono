{
  "name": "@tekclaw/vite-monorepo",
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": "^14.15.0",
    "npm": "^7.17.0"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  },
  "scripts": {
    "start": "npm run start -w ./apps/website",
    "build": "npm run build -ws",
    "clean": "npx -y rimraf node_modules **/node_modules/ package-lock.json && npm i",
    "lint": "npm run lint -ws"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
