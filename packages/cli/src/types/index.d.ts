declare interface GlobalOptions {
  config: string;
  root: string;
  target: string;
  logLevel: string;
  clearScreen: boolean;
  debug: string;
  filter: string;
}

declare interface InitOptions {
  project?: string;
}
